<Page
    x:Class="QISReader.NotenDetailsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QISReader"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer HorizontalScrollMode="Disabled" HorizontalScrollBarVisibility="Disabled">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="40" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="20" />
            </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
            <Frame x:Name="DatenFrame" Grid.Column="1"/>
                <TextBlock x:Name="KeinNotenSpiegelText" Grid.Column="1" Grid.Row="1" TextWrapping="Wrap" Margin="20" FontSize="16" Visibility="Collapsed"/>
        <Frame x:Name="VerteilungsFrame" Grid.Column="1" Grid.Row="1"/>
        <Frame x:Name="BestandenFrame" Grid.Column="1" Grid.Row="2" Margin="80,0,0,0"/>
    </Grid>
    </ScrollViewer>
    
    
</Page>
